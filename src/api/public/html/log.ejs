<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Log Detail – DisBot</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #0e0e11;
      color: #e8e8e8;
      font-family: 'Segoe UI', sans-serif;
    }

    .container {
      max-width: 800px;
    }

    .log-card {
      background: #1a1a1f;
      border: 1px solid #2a2a2e;
      border-radius: 14px;
      padding: 2rem;
      box-shadow: 0 0 20px rgba(0,0,0,0.2);
      margin-top: 2rem;
    }

    .log-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      border-bottom: 1px solid #333;
      padding: 0.8rem 0;
    }

    .log-row:last-child {
      border-bottom: none;
    }

    .log-label {
      font-weight: 600;
      color: #a0a0a0;
      width: 40%;
    }

    .log-value {
      width: 58%;
      text-align: right;
      word-break: break-word;
      color: #f1f1f1;
    }

    .badge-log {
      padding: 0.4rem 0.7rem;
      font-size: 0.85rem;
      border-radius: 0.4rem;
      text-transform: uppercase;
      font-weight: 600;
    }

    .level-error {
      background: #dc3545;
      color: #fff;
    }

    .level-warn {
      background: #ffc107;
      color: #000;
    }

    .level-info {
      background: #0dcaf0;
      color: #000;
    }

    .level-debug {
      background: #6c757d;
      color: #fff;
    }

    .btn-back {
      background-color: #29292f;
      color: #f1f1f1;
      border: none;
    }

    .btn-back:hover {
      background-color: #3a3a44;
    }

    code, pre {
      background-color: #25252a;
      color: #ccc;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      display: block;
      margin: 0;
      font-size: 0.95rem;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <h1 class="text-info mb-4 text-center">📝 Log Detail</h1>

    <% if (!log) { %>
      <div class="alert alert-danger">❌ No logs found</div>
    <% } else { %>
      <div class="log-card">

        <div class="log-row">
          <div class="log-label">UUID</div>
          <div class="log-value"><code><%= log.UUID %></code></div>
        </div>

        <div class="log-row">
          <div class="log-label">Guild</div>
          <div class="log-value"><%= log.GuildId %></div>
        </div>

        <div class="log-row">
          <div class="log-label">User</div>
          <div class="log-value"><%= log.UserId %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Channel</div>
          <div class="log-value"><%= log.ChannelId %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Message</div>
          <div class="log-value"><%= log.MessageId %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Timestamp</div>
          <div class="log-value"><%= new Date(log.Timestamp).toLocaleString() %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Level</div>
          <div class="log-value">
            <span class="badge-log 
              <%= log.Level === 'error' ? 'level-error' :
                   log.Level === 'warn' ? 'level-warn' :
                   log.Level === 'debug' ? 'level-debug' : 'level-info' %>">
              <%= log.Level %>
            </span>
          </div>
        </div>

        <div class="log-row">
          <div class="log-label">Label</div>
          <div class="log-value"><%= log.Label %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Message</div>
          <div class="log-value"><pre><%= log.Message %></pre></div>
        </div>

        <div class="log-row">
          <div class="log-label">Bot Type</div>
          <div class="log-value"><%= log.BotType %></div>
        </div>

        <div class="log-row">
          <div class="log-label">Action</div>
          <div class="log-value"><%= log.Action %></div>
        </div>
      </div>
    <% } %>

    <div class="text-center">
      <a href="/logs" class="btn btn-back mt-4 px-4 py-2">🔙 Back to Overview</a>
    </div>
  </div>
</body>
</html>
