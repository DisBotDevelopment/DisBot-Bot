export enum WebSocketMessageType {
    GUILD = 'guild',
    EVENTSAPI = 'eventsapi',
}

export enum WebSocketAction {
    CREATE = 'create',
    UPDATE = 'update',
    DELETE = 'delete',
    EVENT = 'event',
}