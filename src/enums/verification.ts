export enum VerificationAction {
    AddRole = "add_role",
    AddPermissionToChannel = "add_permission_to_channel",
}

export enum VerificationActionType {
    MathTask = "math_task",
    Reaction = "reaction",
    Button = "button",
    Authorize = "authorize",
    Code = "code",
    Captcha = "captcha",
}